title = "gitleaks config"

[extend]
useDefault = true

[[allowlists]]
description = "Placeholder values in example env files — not real secrets"
paths = [
  '''(^|/)\.env\.example$''',
  '''\.example$''',
]
